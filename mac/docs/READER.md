# Mac minimal reader (mirrors Android)

SwiftUI window: Inbox (attention time `1h 47m`, never counts) / Archive,
title+source+time header, LazyVStack block renderer over the same
ArticleBlock JSON the Android parser emits, same 5 bundled fonts, exactly
Font / Size / Margins / Background (Paper, Soft, Ink, Black), one shared
semantic cursor across scroll + AVSpeechSynthesizer TTS + RSVP with the same
dwell table, export ZIP (md + JSON, never keys). No iCloud sync in v1.
